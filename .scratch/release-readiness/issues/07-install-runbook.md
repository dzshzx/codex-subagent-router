# README 缺持久安装/卸载/trust 可执行 runbook

Status: resolved

缺：持久安装命令（launcher 绝对路径写入 hook，uvx/临时 venv 会留失效路径）、
正确卸载顺序（先 rollback 再卸包）、/hooks trust 审核步骤与安装后 smoke test
（应被拒绝的 child 试探）。

## Answer

> *This was generated by AI during triage.*

commit `ee9af9f` 增加了持久 `uv tool install`、launcher 路径校验、
Hook trust 审核、新 session smoke test 和「先 rollback、后卸包」顺序。
commit `21e27fa` 后续扩展了完整的 plan/install/status 运行前流程。
当前 README 已覆盖本票全部验收面。
