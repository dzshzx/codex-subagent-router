> *This was generated by AI during triage.*

# Codex 0.144.4 installed-binary release gate

Category: enhancement
Status: ready-for-agent

本机当前运行 Codex CLI `0.144.4`，但本项目的不可变兼容性记录只覆盖
`0.144.1` 和 `0.144.3`。当前官方子代理文档把 `agents/*.toml` 列为
custom agent 的主要配置面，而本项目安装器仍生成 description-only
`[agents.<role>]` 声明。安装器 plan 能成功解析配置不等于运行时兼容性
已经证明。

## Agent Brief

**Category:** enhancement
**Summary:** Verify Codex CLI 0.144.4 against the router's complete supported
hook, spawn, role, installation, and recovery contract.

**Current behavior:**
The compatibility matrix has immutable evidence for 0.144.1 and 0.144.3 only.
The production machine now runs 0.144.4, whose current custom-agent discovery
surface may differ from the description-only role declarations generated by the
installer. A successful configuration plan does not establish runtime behavior.

**Desired behavior:**
Treat 0.144.4 as unverified until its official schemas and source behavior have
been compared with the current capability seam and a disposable installed-binary
probe has exercised the full lifecycle. Record the evidence in a new
version-specific artifact. Add 0.144.4 to the compatibility matrix only when all
required observations pass; otherwise leave it unverified and open focused
follow-up issues for observed contract drift.

**Key interfaces:**
- Strict hook input/output protocol for `PreToolUse`, `SessionStart`, and
  `SubagentStart`
- Stable and V2 `spawn_agent` capability shapes, canonical hook tool names, and
  per-spawn model/effort routing
- Managed-role discovery, role precedence, and `SubagentStart` context injection
- User installation plan/install/status/rollback transaction and persistent hook
  launcher
- Hook trust and command-hook failure behavior

**Acceptance criteria:**
- [ ] Compare the 0.144.4 official hook schemas, hook lifecycle, spawn inputs,
      tool-name matching, role precedence, trust behavior, and failure behavior
      with the router contract.
- [ ] In a disposable explicit Codex home, verify root startup guidance,
      deny-before-child-creation, managed child context, generated installation,
      healthy status, and rollback against the installed 0.144.4 binary.
- [ ] Probe description-only inline roles, standalone custom agents, and their
      same-name precedence without reading from or writing to the production
      Codex home.
- [ ] Capture exact observed tool names and spawn shapes instead of inferring
      released-binary behavior from the source tag alone.
- [ ] Add a new immutable version-specific evidence record; update the
      compatibility table only if every release-gate observation passes.
- [ ] Run the repository's complete pre-commit verification suite after any
      required documentation or contract updates.

**Out of scope:**
- Installing the router into the production Codex home
- Silently changing production feature flags or Hook trust
- Adding speculative compatibility fallbacks before a failing observation
- Replacing or rewriting the existing 0.144.1 and 0.144.3 evidence records
