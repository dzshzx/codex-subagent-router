# hide_spawn_agent_metadata=true（帖称默认）下路由字段不可见，validator 退化为 fail-always

Status: needs-triage

帖子（首帖 + openai/codex#31814 截图）：`features.multi_agent_v2.hide_spawn_agent_metadata`
为 `true`（帖称 Codex 默认值）时，`spawn_agent` 对模型可见的 schema 只有
`task_name` / `message` / `fork_turns`；`agent_type` / `model` / `reasoning_effort` /
`service_tier` 被从工具输入里移除，模型无法传。

本项目 validator 把这三个路由字段列为 V2 必填、缺失即 deny
（src/codex_subagent_router/validator.py:26-28）。两者相加：默认配置下模型
自然发出的每次 spawn 都被 deny——整套路由从 fail-closed 退化为 fail-always。

本仓库旁证：docs/research/codex-0.144.3-hook-evidence.md:63 探针记录的
「自然模型调用形状 `{task_name, message, fork_turns="none"}`」恰好与
hide=true 的 schema 症状一致（不带任何路由字段）。全仓 rg 无
`hide_spawn_agent_metadata` 命中——此配置项是现有研究盲区。

待办：

1. installed-binary 探针核实：该配置项在支持矩阵内二进制的真实默认值，
   以及 true/false 两态下模型可见 schema 与自然调用形状。
2. 若属实，`hide_spawn_agent_metadata = false` 是本项目显式路由的硬性运行
   前提。修复面待定（needs-triage 的原因）：preflight/plan 列为 conflict、
   status 报不健康、还是只写进安装 runbook；安装器是否主动写该键触及
   「不静默接管用户配置」硬性约束，需用户定。
3. 与票 02 联动：hide=false 恰好落入 #31864 的冲突组合，两条前提必须一起校验。

## Comments

> *This was generated by AI during triage.*

2026-07-15 本机样本：Codex CLI `0.144.4` 的用户 config 已显式设置
`features.multi_agent_v2.hide_spawn_agent_metadata = false`，但
`codex features list` 报告 `multi_agent_v2` 仍为关闭。这证明本机已预置
票中候选的运行参数，但不证明默认值、V2 开启后 schema 或自然调用形状；
本票仍保持 `needs-triage`。
